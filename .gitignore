# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
.output
.vinxi
.tanstack/**
.nitro
*.local

# SSR bundle mirror — produced by scripts/copy-ssr-output.mjs so the Vercel
# function tracer can follow the relative import from api/index.js.
api/_ssr

# Wrangler / Cloudflare
.wrangler/
.dev.vars

# Claude Code local state (scheduling lock, etc.)
.claude/

# design-sync (claude.ai/design) — staged scripts, build output, machine state.
# Durable inputs under .design-sync/ (config.json, NOTES.md, conventions.md,
# previews/, overrides/) ARE committed; only these generated bits are ignored.
.ds-sync/
ds-bundle/
.design-sync/.cache/
.design-sync/learnings/
.design-sync/node_modules

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.vercel
